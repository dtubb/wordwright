# Compatibility shim

Primary project instructions live at the repo root.

From this `.claude/` directory, use:
- `../CLAUDE.md` for the repo entrypoint
- `../STATE.md` if present, for current state
- `../MEMORY.md` if present, for durable project context
- `../TASKS.md` if present, for backlog or active work

This `.claude/` directory only holds tool-local configuration that must remain here.
