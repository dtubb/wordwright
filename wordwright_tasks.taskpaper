WordWright Enhancements:
	- Integrate LLM for Text Refinement @tags(LLM, refinement, AI) 
		- Send cleaned text to ChatGPT or LM Studio with an editing prompt
		- Retrieve the refined version
		- Add a flag to enable/disable this step
	- DeepL Integration (Optional) @tags(translation, DeepL)
		- Send text to DeepL for translation
		- Return translated or refined text
	- Remove Extraneous Characters @tags(cleanup, formatting)
		- Normalize dashes, hyphens, curly quotes, and apostrophes
		- Strip unwanted characters
	- Handle Output Options @tags(output, clipboard, file)
		- Save final processed text to a file
		- Copy cleaned text to clipboard
		- Print cleaned text if run in CLI
	- Add CLI Flags for Customization @tags(CLI, flags)
		- Enable `--no-llm` flag to skip LLM refinement
		- Enable `--no-phrases` flag to skip phrase removal
		- Add CLI options for input/output control
	- Integrate with Keyboard Maestro @tags(automation, Keyboard Maestro)
		- Create a macro to trigger `wordwright.py` with clipboard text
		- Automate clipboard replacement
	- Optimize Performance @tags(performance, optimization)
		- Improve regex efficiency
		- Consider multithreading for large text processing

